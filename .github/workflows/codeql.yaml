name: CodeQL

permissions:
  contents: read

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "23 4 * * 1"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 120
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: swift
            os: macos-26
            build-mode: manual
          - language: actions
            os: ubuntu-latest
            build-mode: none
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Xcode
        if: matrix.language == 'swift'
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Install swiftly
        if: matrix.language == 'swift'
        run: brew install swiftly && swiftly init --quiet-shell-followup --skip-install -y

      # CodeQL's Swift extractor wraps every swiftc invocation via
      # DYLD_INSERT_LIBRARIES once `init` runs. Compiling SwiftProtobuf
      # (~100 files) under that tracer would push the runner past its
      # memory/timeout limits, so build it — and resolve the binary
      # xcframeworks — before init. The traced step then only has to
      # compile our own code.
      - name: Pre-build dependencies
        if: matrix.language == 'swift'
        run: swiftly run +xcode swift build --target SwiftProtobuf

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      - name: Build LiveKit
        if: matrix.language == 'swift'
        env:
          NSUnbufferedIO: "YES"
        run: swiftly run +xcode swift build --target LiveKit

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{ matrix.language }}"
