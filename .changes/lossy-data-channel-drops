patch type="changed" "The lossy data channel now drops the oldest queued payload under sustained backpressure (more than 2MB buffered) instead of queueing without bound; a dropped send returns normally, and drops are counted and logged"
