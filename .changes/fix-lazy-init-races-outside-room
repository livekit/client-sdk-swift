patch type="fixed" "Fix data races on lazily initialized members outside Room (DeviceManager's discovery session, AudioManager's processing delegate adapters, CameraCapturer's RTC capturer and delegate adapter, E2EEManager's frame cryptor delegate adapter): concurrent first access could run a lazy initializer twice; they are now created exactly once, in the initializer or on the single queue that owns them"
